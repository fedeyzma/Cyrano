# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
next-env.d.ts

# production / local data (SQLite lives here)
/data
*.db
*.db-shm
*.db-wal

# personal context (keep fred_context.example.md tracked)
/fred_context.md

# env files (keep .env.example tracked)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# personal handoff / working notes (deployment specifics, not for the repo)
/HANDOFF.md

# misc
*.log
npm-debug.log*
.DS_Store
.vercel
*.tsbuildinfo
